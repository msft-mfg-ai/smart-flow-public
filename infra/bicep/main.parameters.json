{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
      "azdEnvName": {
        "value": "${AZURE_ENV_NAME}"
      },
      "applicationName": {
        "value": "${AZURE_ENV_NAME}"
      },
      "location": {
        "value": "${AZURE_LOCATION}"
      },
      "principalId": {
        "value": "${AZURE_PRINCIPAL_ID}"
      },
      "uiImageName": {
        "value": "${SERVICE_UI_IMAGE_NAME}"
      },
      "apiImageName": {
        "value": "${SERVICE_API_IMAGE_NAME}"
      },
      "batchImageName": {
        "value": "${SERVICE_BATCH_IMAGE_NAME}"
      },
      "myIpAddress": {
        "value": "${MY_IP}"
      },
      "addRoleAssignments" : {
        "value": "${ADD_ROLE_ASSIGNMENTS}"
      },
      "createDnsZones" : {
        "value": "${CREATE_DNS_ZONES}"
      },
      "publicAccessEnabled": {
        "value": "${PUBLIC_ACCESS_ENABLED}"
      },
      "existingVnetName": {
        "value": "${VNET_NAME}"
      },
      "subnet1Name": {
        "value": "${SUBNET_PE_NAME}"
      },
      "subnet2Name": {
        "value": "${SUBNET_APP_NAME}"
      },
      "searchServiceSkuName": {
        "value": "${AZURE_SEARCH_SERVICE_SKU=basic}"
      },
      "searchIndexName": {
        "value": "${AZURE_SEARCH_INDEX=voicerag-intvect}"
      },
      "searchSemanticConfiguration":{
        "value": "${AZURE_SEARCH_SEMANTIC_CONFIGURATION=default}"
      },
      "searchIdentifierField": {
        "value": "${AZURE_SEARCH_IDENTIFIER_FIELD=chunk_id}"
      },
      "searchContentField": {
        "value": "${AZURE_SEARCH_CONTENT_FIELD=chunk}"
      },
      "searchTitleField": {
        "value": "${AZURE_SEARCH_TITLE_FIELD=title}"
      },
      "searchEmbeddingField": {
        "value": "${AZURE_SEARCH_EMBEDDING_FIELD=text_vector}"
      },
      "searchUseVectorQuery": {
        "value": "${AZURE_SEARCH_USE_VECTOR_QUERY=true}"
      },
      "searchServiceSemanticRankerLevel": {
        "value": "${AZURE_SEARCH_SEMANTIC_RANKER=free}"
      }
    }
  }